{
  "file": "examples/demo.ts",
  "generated": "2026-06-16",
  "assumesVerified": true,
  "summary": {
    "contracts": 2,
    "confirmed": 1,
    "disputed": 1,
    "gaps": 1
  },
  "guarantees": [
    {
      "specId": "clamp",
      "function": "clamp",
      "requirement": "Clamps x into the inclusive range [lo, hi]; the result never falls outside it.",
      "spec": {
        "signature": "clamp(x: number, lo: number, hi: number): number",
        "requires": [
          "lo <= hi"
        ],
        "ensures": [
          "\\result >= lo && \\result <= hi"
        ]
      },
      "status": "confirmed",
      "backTranslation": "The clamp function takes three numbers (x, lo, hi) and returns a number that is greater than or equal to lo and less than or equal to hi."
    },
    {
      "specId": "largest",
      "function": "largest",
      "requirement": "Returns the larger of a and b.",
      "spec": {
        "signature": "largest(a: number, b: number): number",
        "requires": [],
        "ensures": [
          "\\result >= a"
        ]
      },
      "status": "disputed",
      "weakeningType": "weakened-postcondition",
      "discrepancy": "The requirement says the function returns the larger of a and b, which means the result must be >= a AND >= b (i.e., it is at least as large as both inputs). The lemma only ensures result >= a, completely omitting the condition result >= b. Furthermore, 'the larger of a and b' implies the result equals max(a, b), not merely some value >= a. The lemma is far too weak.",
      "backTranslation": "The largest function takes two numbers (a, b) and returns a number that is greater than or equal to a."
    }
  ],
  "gaps": [
    {
      "specId": "double",
      "requirement": "Doubles its input.",
      "reason": "//@ contract present but no //@ requires/ensures to back it"
    }
  ]
}
